@startuml Stage_1_ModelArchitecture

title Stage 1: Neural LOD Controller Architecture (Horizontal Layout)

skinparam componentStyle rectangle
skinparam defaultTextAlignment left
skinparam component {
  BackgroundColor #FAFAFA
  BorderColor #333
}

' --- 1. DEFINITIONS (LOSSLESS) ---

component "**Input Layer**\n19 Normalized Features:\n- CPU/GPU times, FPS, Headroom\n- Triangle count, Draw calls\n- Cam Velocity/Rot, Coverage\n- Pos (x,y,z), Rot (y)\n- Path/Waypoint index\n- Move/Rotate speeds" as Input

component "**Layer 1** (Dense)\n- Shape: 19 → 64\n- ReLU activation\n- 1,280 parameters" as L1

component "**Layer 2** (Dense)\n- Shape: 64 → 32\n- ReLU activation\n- 2,080 parameters" as L2

component "**Layer 3** (Dense)\n- Shape: 32 → 16\n- ReLU activation\n- 528 parameters" as L3

component "**Output Head**\n- Shape: 16 → 1\n- Sigmoid activation\n- Range: [0.25, 2.0]\n- 17 parameters\n- **TOTAL PARAMS: 3,905**" as Output

component "**Guards & Export**\n- Label oracle: target_lod_bias\n- Normalization: StandardScaler\n- Export: ONNX format\n- Scaler JSON (Mean/Scale)" as Guards

' --- 2. HORIZONTAL LAYOUT ---

Input -r-> L1
L1 -r-> L2
L2 -r-> L3
L3 -r-> Output
Output -r-> Guards

@enduml