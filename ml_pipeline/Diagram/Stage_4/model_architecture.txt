@startuml Stage_4_ModelArchitecture

title Stage 4: REINFORCE RL Policy Network (Snake Layout)

skinparam componentStyle rectangle
skinparam defaultTextAlignment left
skinparam component {
  BackgroundColor #FAFAFA
  BorderColor #333
}

' --- 1. LAYER & STATE DEFINITIONS ---

component "**Input State** (13 Normalized Features)\n- CPU/GPU ms, FPS, Renderers\n- Triangles, Draw Calls\n- Cam Speed/Rot, Screen Coverage\n- Prev Bias, Switch Count\n- Floor/Ceiling Dwell Scores" as State

component "**Layer 1** (Dense)\n- Shape: 13 → 256\n- ReLU + Dropout\n- 3,328 parameters" as L1

component "**Layer 2** (Dense)\n- Shape: 256 → 64\n- ReLU + Dropout\n- 16,448 parameters" as L2

component "**Layer 3** (Dense)\n- Shape: 64 → 32\n- ReLU + Dropout\n- 2,048 parameters" as L3

component "**Action Head**\n- Shape: 32 → 1 (Linear)\n- log_sigma param\n- Total: 21,857 params\n- Unbounded raw action" as Action

component "**Post-Proc: Stability Stack**\n1. **Deadzone**: Stop GPU noise\n2. **EMA**: Alpha 0.2 smoothing\n3. **Dwell**: 0.5s lock timer\n4. **Clamp**: [0.3, 2.0] Guardrails" as Filters

' --- 2. SNAKE LAYOUT ---

' Row 1: Right
State -r-> L1
L1 -r-> L2

' Turn 1: Down
L2 -d-> L3

' Row 2: Left
L3 -l-> Action
Action -l-> Filters

note bottom of State
  **TWITCHY GENIUS FIX:**
  Raw policy output: 3Hz oscillation
  After filters: 0.3-0.5Hz stable
  Net result: +1.2 ms GPU savings
end note

note top of Action
  **POLICY HYPERPARAMS:**
  - REINFORCE (Policy Gradient)
  - Gamma: 0.99
  - Batch: 256
  - Optuna: 15 trials
end note

@enduml